name: Activate 2

on:
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true
  ACTIONS_RUNNER_DEBUG: true

jobs:
  step01:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run normalize_lineups.py
        run: python scripts/normalize_lineups.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 01: Ran normalize_lineups.py"
          git push || echo "No changes to commit"

  step02:
    needs: step01
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run normalize_todays_games.py
        run: python scripts/normalize_todays_games.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 02: Ran normalize_todays_games.py"
          git push || echo "No changes to commit"

  step03:
    needs: step02
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_team_name_mapping.py
        run: python scripts/apply_team_name_mapping.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 03: Ran apply_team_name_mapping.py"
          git push || echo "No changes to commit"

  step04:
    needs: step03
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run update_game_time.py
        run: python scripts/update_game_time.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 04: Ran update_game_time.py"
          git push || echo "No changes to commit"

  step05:
    needs: step04
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run generate_weather_csv.py
        run: python scripts/generate_weather_csv.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 05: Ran generate_weather_csv.py"
          git push || echo "No changes to commit"

  step06:
    needs: step05
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run get_weather_data.py
        run: python scripts/get_weather_data.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 06: Ran get_weather_data.py"
          git push || echo "No changes to commit"

  step07:
    needs: step06
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run filter_today_batters.py
        run: python scripts/filter_today_batters.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 07: Ran filter_today_batters.py"
          git push || echo "No changes to commit"

  step08:
    needs: step07
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run split_batters_by_home_away.py
        run: python scripts/split_batters_by_home_away.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 08: Ran split_batters_by_home_away.py"
          git push || echo "No changes to commit"

  step09:
    needs: step08
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run split_pitchers.py
        run: python scripts/split_pitchers.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 09: Ran split_pitchers.py"
          git push || echo "No changes to commit"

  step10:
    needs: step09
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_weather_adjustment.py
        run: python scripts/apply_weather_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 10: Ran apply_weather_adjustment.py"
          git push || echo "No changes to commit"

(continued in next message)

Here is the continuation of the updated `activate_2.yml` from **step11 to step18**, each with full `git add/commit/push` logic:

```yaml
  step11:
    needs: step10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_park_adjustment.py
        run: python scripts/apply_park_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 11: Ran apply_park_adjustment.py"
          git push || echo "No changes to commit"

  step12:
    needs: step11
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_weather_park_away.py
        run: python scripts/combine_weather_park_away.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 12: Ran combine_weather_park_away.py"
          git push || echo "No changes to commit"

  step13:
    needs: step12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_weather_park_home.py
        run: python scripts/combine_weather_park_home.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 13: Ran combine_weather_park_home.py"
          git push || echo "No changes to commit"

  step14:
    needs: step13
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_pitcher_weather_adjustment.py
        run: python scripts/apply_pitcher_weather_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 14: Ran apply_pitcher_weather_adjustment.py"
          git push || echo "No changes to commit"

  step15:
    needs: step14
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_pitcher_park_adjustment.py
        run: python scripts/apply_pitcher_park_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 15: Ran apply_pitcher_park_adjustment.py"
          git push || echo "No changes to commit"

  step16:
    needs: step15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_pitcher_weather_park_home.py
        run: python scripts/combine_pitcher_weather_park_home.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 16: Ran combine_pitcher_weather_park_home.py"
          git push || echo "No changes to commit"

  step17:
    needs: step16
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_pitcher_weather_park_away.py
        run: python scripts/combine_pitcher_weather_park_away.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 17: Ran combine_pitcher_weather_park_away.py"
          git push || echo "No changes to commit"

  step18:
    needs: step17
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run build_matchup_stats.py
        run: python scripts/build_matchup_stats.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Step 18: Ran build_matchup_stats.py"
          git push || echo "No changes to commit"
