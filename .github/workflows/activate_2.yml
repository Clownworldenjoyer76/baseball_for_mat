name: Activate 2

on:
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true
  ACTIONS_RUNNER_DEBUG: true

jobs:
  step01:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Normalize Today's Lineups
        run: |
          echo "🔧 Starting normalize_todays_lineups.py..."
          python scripts/normalize_todays_lineups.py

  step02:
    needs: step01
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Normalize Today's Games
        run: |
          echo "🔧 Starting normalize_todays_games.py..."
          python scripts/normalize_todays_games.py

  step03:
    needs: step02
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Standardize Team Names
        run: |
          echo "🔧 Starting standardize_team_names.py..."
          python scripts/standardize_team_names.py

  step04:
    needs: step03
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Update Game Time
        run: |
          echo "🔧 Starting update_game_time.py..."
          python scripts/update_game_time.py

  step05:
    needs: step04
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Generate Weather CSV
        run: |
          echo "🔧 Starting generate_weather_csv.py..."
          python scripts/generate_weather_csv.py

  step06:
    needs: step05
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Fetch Weather Data
        run: |
          echo "🔧 Starting get_weather_data.py..."
          python scripts/get_weather_data.py

  step07:
    needs: step06
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Filter Today Batters
        run: |
          echo "🔧 Starting filter_today_batters.py..."
          python scripts/filter_today_batters.py

  step08:
    needs: step07
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Split Batters Home/Away
        run: |
          echo "🔧 Starting split_batters_home_away.py..."
          python scripts/split_batters_home_away.py

  step09:
    needs: step08
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Split Pitchers
        run: |
          echo "🔧 Starting split_pitchers.py..."
          python scripts/split_pitchers.py

  step10:
    needs: step09
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Apply Weather Adjustment
        run: |
          echo "🔧 Starting apply_weather_adjustment.py..."
          python scripts/apply_weather_adjustment.py

  step11:
    needs: step10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Apply Park Adjustment
        run: |
          echo "🔧 Starting apply_park_weather_adjustment_modular.py..."
          python scripts/apply_park_weather_adjustment_modular.py

  step12:
    needs: step11
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Combine Weather + Park Away
        run: |
          echo "🔧 Starting combine_weather_park_away.py..."
          python scripts/combine_weather_park_away.py

  step13:
    needs: step12
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Combine Weather + Park Home
        run: |
          echo "🔧 Starting combine_weather_park_home.py..."
          python scripts/combine_weather_park_home.py

  step14:
    needs: step13
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Apply Pitcher Weather Adjustment
        run: |
          echo "🔧 Starting apply_pitcher_weather_adjustment.py..."
          python scripts/apply_pitcher_weather_adjustment.py

  step15:
    needs: step14
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Apply Pitcher Park Adjustment
        run: |
          echo "🔧 Starting apply_pitcher_park_adjustment.py..."
          python scripts/apply_pitcher_park_adjustment.py

  step16:
    needs: step15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Combine Pitcher Weather + Park Home
        run: |
          echo "🔧 Starting combine_pitcher_weather_park_home.py..."
          python scripts/combine_pitcher_weather_park_home.py

  step17:
    needs: step16
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Combine Pitcher Weather + Park Away
        run: |
          echo "🔧 Starting combine_pitcher_weather_park_away.py..."
          python scripts/combine_pitcher_weather_park_away.py

  step18:
    needs: step17
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install pandas
        run: pip install pandas
      - name: Build Matchup Stats
        run: |
          echo "🔧 Starting build_matchup_stats.py..."
          python scripts/build_matchup_stats.py