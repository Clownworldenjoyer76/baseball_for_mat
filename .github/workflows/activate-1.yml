name: Activate 1

on:
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true
  ACTIONS_RUNNER_DEBUG: true

jobs:
  activate_1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install pandas
        run: pip install pandas

      - name: Create Summary File
        run: |
          mkdir -p summaries
          echo "✅ normalize_names.py ran successfully." > summaries/activate_1_summary.txt
          echo "🔄 Modified: data/normalized/batters_normalized.csv" >> summaries/activate_1_summary.txt
          echo "🔄 Modified: data/normalized/pitchers_normalized.csv" >> summaries/activate_1_summary.txt

      - name: Run normalize_names.py
        run: python scripts/normalize_names.py

      - name: Run tag_master_files.py
        run: |
          python scripts/tag_master_files.py
          echo "✅ tag_master_files.py ran successfully." >> summaries/activate_1_summary.txt
          echo "🔄 Modified: data/tagged/batters_normalized.csv" >> summaries/activate_1_summary.txt
          echo "🔄 Modified: data/tagged/pitchers_normalized.csv" >> summaries/activate_1_summary.txt

      - name: Run deduplicate_normalized.py
        run: |
          python scripts/deduplicate_normalized.py
          echo "✅ deduplicate_normalized.py ran successfully." >> summaries/activate_1_summary.txt
          echo "🔄 Modified: data/cleaned/batters_normalized_cleaned.csv" >> summaries/activate_1_summary.txt
          echo "🔄 Modified: data/cleaned/pitchers_normalized_cleaned.csv" >> summaries/activate_1_summary.txt

      - name: Commit & Push Summary
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add summaries/activate_1_summary.txt
          git commit -m "📄 activate_1_summary.txt written" || echo "No changes to commit"
          git push || echo "Nothing to push"
