name: Activate 4

on:
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true
  ACTIONS_RUNNER_DEBUG: true

jobs:
  step13:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_weather_park_home.py
        run: |
          echo "🔧 Starting combine_weather_park_home.py..."
          python scripts/combine_weather_park_home.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git diff --cached --quiet || git commit -m "Step 13: Ran combine_weather_park_home.py"
          git push || echo "No changes to commit or push"

  step14:
    needs: step13
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_pitcher_weather_adjustment.py
        run: |
          echo "🔧 Starting apply_pitcher_weather_adjustment.py..."
          python scripts/apply_pitcher_weather_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git diff --cached --quiet || git commit -m "Step 14: Ran apply_pitcher_weather_adjustment.py"
          git push || echo "No changes to commit or push"

  step15:
    needs: step14
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_pitcher_park_adjustment.py
        run: |
          echo "🔧 Starting apply_pitcher_park_adjustment.py..."
          python scripts/apply_pitcher_park_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git diff --cached --quiet || git commit -m "Step 15: Ran apply_pitcher_park_adjustment.py"
          git push || echo "No changes to commit or push"

  step16:
    needs: step15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_pitcher_weather_park_home.py
        run: |
          echo "🔧 Starting combine_pitcher_weather_park_home.py..."
          python scripts/combine_pitcher_weather_park_home.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git diff --cached --quiet || git commit -m "Step 16: Ran combine_pitcher_weather_park_home.py"
          git push || echo "No changes to commit or push"

  step17:
    needs: step16
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_pitcher_weather_park_away.py
        run: |
          echo "🔧 Starting combine_pitcher_weather_park_away.py..."
          python scripts/combine_pitcher_weather_park_away.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git diff --cached --quiet || git commit -m "Step 17: Ran combine_pitcher_weather_park_away.py"
          git push || echo "No changes to commit or push"

  step18:
    needs: step17
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run build_matchup_stats.py
        run: |
          echo "🔧 Starting build_matchup_stats.py..."
          python scripts/build_matchup_stats.py
      - name: Commit and Push Changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git diff --cached --quiet || git commit -m "Step 18: Ran build_matchup_stats.py"
          git push || echo "No changes to commit or push"
