
name: Activate 4

on:
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true
  ACTIONS_RUNNER_DEBUG: true

jobs:
  step13:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_weather_park_home.py
        run: |
          echo "🔧 Starting combine_weather_park_home.py..."
          python scripts/combine_weather_park_home.py
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add -A
          git commit -am "Step 13: Ran combine_weather_park_home.py" || echo "No changes to commit"
          git push || echo "Nothing to push"

  step14:
    needs: step13
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_pitcher_weather_adjustment.py
        run: |
          echo "🔧 Starting apply_pitcher_weather_adjustment.py..."
          python scripts/apply_pitcher_weather_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add -A
          git commit -am "Step 14: Ran apply_pitcher_weather_adjustment.py" || echo "No changes to commit"
          git push || echo "Nothing to push"

  step15:
    needs: step14
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_pitcher_park_adjustment.py
        run: |
          echo "🔧 Starting apply_pitcher_park_adjustment.py..."
          python scripts/apply_pitcher_park_adjustment.py
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add -A
          git commit -am "Step 15: Ran apply_pitcher_park_adjustment.py" || echo "No changes to commit"
          git push || echo "Nothing to push"

  step16a:
    needs: step15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run apply_pitcher_park_adjustment_step1.py
        run: |
          echo "🔧 Starting apply_pitcher_park_adjustment_step1.py..."
          python scripts/apply_pitcher_park_adjustment_step1.py
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add -A
          git commit -am "Step 16a: Ran apply_pitcher_park_adjustment_step1.py" || echo "No changes to commit"
          git push || echo "Nothing to push"

  step16b:
    needs: step16a
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_pitcher_weather_park_home_step2.py
        run: |
          echo "🔧 Starting combine_pitcher_weather_park_home_step2.py..."
          python scripts/combine_pitcher_weather_park_home_step2.py
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add -A
          git commit -am "Step 16b: Ran combine_pitcher_weather_park_home_step2.py" || echo "No changes to commit"
          git push || echo "Nothing to push"

  step17:
    needs: step16b
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run combine_pitcher_weather_park_away.py
        run: |
          echo "🔧 Starting combine_pitcher_weather_park_away.py..."
          python scripts/combine_pitcher_weather_park_away.py
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add -A
          git commit -am "Step 17: Ran combine_pitcher_weather_park_away.py" || echo "No changes to commit"
          git push || echo "Nothing to push"

  step18:
    needs: step17
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: pip install pandas requests pytz
      - name: Run build_matchup_stats.py
        run: |
          echo "🔧 Starting build_matchup_stats.py..."
          python scripts/build_matchup_stats.py
      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add -A
          git commit -am "Step 18: Ran build_matchup_stats.py" || echo "No changes to commit"
          git push || echo "Nothing to push"
